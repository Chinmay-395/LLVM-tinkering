Things I need to revise for LLVM 

It gives you:

functions,
basic blocks,
labels,
branches and conditional branches,
local SSA identifiers,
global identifiers,
pointer types,
structs and arrays,
alloca,
load,
store,
getelementptr,
bitcast,
direct and indirect call,
function pointers,
global initializers.